# Server
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL=postgres://sofa_db:quocbao1123@localhost:5434/sofa_db

# JWT
JWT_ACCESS_SECRET=JWT_ACCESS_SECRET(change-this)

JWT_REFRESH_SECRET=JWT_REFRESH_SECRET(change-this)
# (Tuỳ chọn, sau này dùng)
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
